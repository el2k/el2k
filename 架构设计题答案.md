模块一：Context / Performance
1.大模型面对第一轮长窗口或多模态输入时，first token 会显著变慢。有什么快速/低成本/用户体验也不差的方案？从5-10秒稳定压缩到2秒。
    首Token延迟（TTFT）长的根本原因在于：长上下文意味着海量的KV Cache计算和注意力矩阵运算，而多模态输入（如图片）则需要经过庞大的Vision Transformer（ViT）编码器，计算量随Token数呈线性甚至平方级增长。
    这恰恰是跟我的研究方向有点关系
    第一，用轻量级检索或小模型摘要对用户的长输入进行前置压缩，将上下文从万级Token降至千级，控制主模型的输入规模；
    第二，对多模态输入采用‘视觉转文本’策略，减少海量图像Token的产生；
    第三，配合推理引擎的前缀缓存，避免重复计算系统指令。
    这样就导致首轮会快速响应，但后续用户询问细节时，可能不精准。
    比如视觉转文本，但可以在后台异步并行处理高分辨率版本，并将高精度图片产生的完整KV Cache（键值缓存）存入Redis，不释放。
    强制要求小模型输出一个原子事实清单，每个事实都是一个独立的、不可再分的单位，方便后续检索和使用。
    前端加一层极轻量级的意图分类器（几十毫秒），将用户问题分为两类，延续性/总结性追问，事实性/细节性追问，后者通过强制降级，
    直接跳过缓存的摘要，携带完整原始上下文或指向原始存储的指针，走全量精度推理。

2.一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？
    我目前的想法: 
        1.通过提取前面信息为摘要，只保存摘要信息，而不是所有对话内容。同时可以使用embedding技术，将摘要转换为向量表示，方便后续检索，摘要达到一定长度后，再对摘要进行二次压缩（摘要的摘要）
        摘要可以调用 LLM 生成结构化摘要
        2.可以考虑使用分块存储，将对话内容分块存储，每个块的大小为 1000 个 token。当需要检索时，根据用户的问题，从存储中提取相关块，再进行合并。
        3.分层记忆，企业级的长上下文管理，本质上是一个将被动压缩转变为主动记忆管理的系统工程。核心是构建一个分层记忆体系，将上下文视为需要精细化预算的有限资源。这包括：L0核心缓冲区（保留最近2-3轮完整对话）、L1短期记忆（滑动窗口保留近期轮次）、L2长期记忆（通过向量数据库存储从历史中提取的结构化信息，如用户偏好与关键事实）以及L3外部知识库（结合RAG检索企业静态知识）。在实际运行时，我们通过智能检索按需召回L2和L3的相关信息注入上下文，并对工具输出等大块内容进行前置压缩，同时为系统提示、历史对话、检索结果等不同部分分配Token预算，确保上下文总在可控范围内。这个方案解决了单纯摘要压缩导致的信息丢失问题，使AI助手在数百轮对话后依然能准确“记住”关键信息，同时具备多租户隔离、持久化存储和可观测性等企业级特性，是目前构建可靠、高性能AI应用的基础设施。

模块二：Memory
  1.和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？
    这里已经不太适合向量检索或者传统检索的方法去召回，因为用户的问题可能已经变化，或者用户的问题可能已经超出了上下文的范围。
    第一，意图去重+时效校验，检测到重复问题后，不直接吐旧结果，而是检查该意图对应的客观状态是否已变，优先用实时数据覆盖旧记忆；
    第二，区分召回通道，闲聊回忆只搜‘事件流’，关键参数只搜‘属性快照’，半个月沉淀后的用户画像比原始问答准确得多；
    第三，增量过滤，把已解决的记忆打标签并降权，重复问题时自动忽略已完结内容，只召回未闭环的最新片段；
    第四，响应策略，不机械复读旧答案，而是以‘历史背景+当前状态’的方式组织，并主动反问用户是否有变更。

  2.你理解的 Agent memory 经典框架是什么？它的发展趋势是什么，最头部的玩家在怎么做？
    Mem0：极简的“记忆即服务”：它像一个可插拔的“记忆外挂”，提供极简的 add() 和 search() API，对上层应用框架（如LangChain）无依赖。其核心特点是框架无关、接入成本极低，能在15分钟内完成接入，适合快速验证概念（MVP）。目前它在GitHub上已拥有超过48K的Star。
    Agent Memory正从一个辅助功能，演变为决定AI Agent能否从“聊天工具”走向“行动系统”的关键基础设施。它不仅解决了大模型“健忘”的痛点，更通过分层架构、主动调度、认知建模等创新，为AI赋予了持续学习和适应能力。
    这一领域的核心挑战已从“如何存储”转向“如何管理、评估和安全地遗忘”。未来的Agent将不仅仅是执行指令的工具，更是能够积累经验、与用户共同成长的智能伙伴。

模块三：Task 
1.对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？
    -- 将原始用户目标进行压缩摘要，作为“系统级指令”常驻System Prompt的最前端（固定头部的注意力权重最高）。同时，每执行N步（如5步），强制插入一条“重读指令”：“请重新审视最顶层的原始目标，确认当前计划是否偏移。”
    该方法的优点是简单、有效，能够及时发现并纠正目标偏移问题。缺点是需要用户手动触发，且在任务执行过程中，用户需要保持关注任务进度，以确保目标不被忘掉。

    -- 首次启动时，让大模型生成一份结构化的子任务清单（JSON数组），存入外部存储。每次执行完一步，Agent必须调用check_plan()工具，将该子任务标记为completed，并读取下一步。上下文只存当前子任务，总目标在外部“看板”上。
    该方法的优点是能够及时发现并纠正目标偏移问题，同时能够自动切换到下一个子任务，以确保目标不被忘掉。
    现实任务充满变数（如途中用户改变了主意），一旦初始规划错误，Agent会“错误地努力到底”。

    -- 状态检查点与回滚
    源自LangGraph等编排框架。每完成一个里程碑，将当前的完整状态（消息列表、变量）序列化存入数据库。当目标检测到偏移时，不依赖模型“回忆”，而是直接回滚到最近的成功检查点，加载当时的状态重新规划路径。
    优点：容灾性强，避免因单步错误导致整个任务崩溃；支持人机协同（用户可手动选择回滚点）。
    缺点：存储成本随步数线性增长；在非确定性任务中（如创意写作），回滚可能丢失关键灵感。

    -- 维护两个独立的Agent。Planner（规划者）拥有完整的长期上下文和原始目标，仅负责宏观路径调整，低频运行（如每10步唤醒一次）；Executor（执行者）只携带当前子任务和最近2步历史，高频运行，负责落地。两者通过外部消息队列通信。
    优点：彻底隔离“目标保持”与“动作执行”，上下文污染最小，是目前OpenAI、Anthropic等头部企业内部构建复杂工作流的标准范式。
    缺点：架构极重，系统复杂度和调试难度极高；通信同步机制（如Planner唤醒时对Executor的打断处理）需要精妙设计。

        
2.用户给 Agent 下达任务：每天早上 9 点根据昨天聊天情况做复盘总结。你会怎么设计？
    我会把它设计成一个‘离线智能体’而不是单纯的定时脚本。核心在于窗口治理和结构化提取：
   首先，利用分布式任务调度确保9点整精准触发，并做好幂等和补跑机制。拿到昨天的历史数据后，我不会无脑全量喂给模型，而是先分层提取：工具执行结果和明确指令归为‘硬事实’，闲聊情绪和客套话归为‘软摘要’，用不同路径处理。
   其次，复盘输出必须是结构化的JSON，强制包含‘待办事项’、‘偏好更新’和‘建议动作’，这保证了AI给出的不仅仅是‘回忆录’，而是可执行的‘待办清单’。
   最后，这份复盘会作为一条带时间戳的长时记忆写入向量库，同时增量更新用户画像（比如覆盖最新预算偏好，追加新待办）。并且我会主动推送通知，让用户看到‘软通知’+‘硬按钮’，实现从‘总结过去’到‘驱动今天’的闭环。为了应对长周期，我还会引入前3天的历史复盘作为上下文参考，防止总结出现短期遗忘。

模块四：Tool / Session Runtime
1.Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？
    -- 异步工具执行时，Agent会先将当前状态设置为 busy，同时触发工具调用。工具执行完成后，Agent会将状态设置为 idle，同时将工具输出写入会话历史。
    -- 异步工具完成通知，Agent会将当前状态设置为 idle，同时触发工具调用结果的处理。
    这里一般使用Celery等任务队列，将工具调用结果作为任务，异步处理。
    通知则使用WebSocket等实时通信技术，将结果推送给用户。

2.如果 session state 为 busy，此时用户又发来新消息，或者异步工具完成事件也到达，runtime 应该如何处理？
    当会话状态为 busy 时，runtime 应当采用“带优先级的事件队列 + 非阻塞状态提示”策略，而不是直接拒绝或丢弃。具体来说：所有新到达的事件（包括用户消息和异步工具回调）统一进入一个 FIFO 队列，但用户消息在入队时立即返回一个“系统正在处理中，请稍候”的流式占位符（如“收到，我处理完当前任务马上看您的新消息”），以防止用户端超时或重复发送；而异步工具完成事件必须严格入队并保留原始上下文 ID，绝不能因状态繁忙而丢失或自动重试，否则会导致工作流永久挂起。当当前任务结束释放 busy 状态时，runtime 按队列顺序依次消费事件，但需要优先消费工具回调（因为它是上一轮工作的延续，需先落地才能保证数据一致性），再处理用户的新消息，并在此过程中利用分布式锁和状态版本号防止并发冲突，确保每个事件在原子事务内完成处理。

模块五：Agent Runtime 架构对比
1.Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？
    关于工具调用方式：Claude Code 与 GLM/豆包等模型的核心差异在于设计哲学——Claude Code 将工具视为一套“受控执行协议”，强调对工具调用的深度治理，通过权限校验、并发控制、错误归一化和流式交互等机制，将工具调用类比为操作系统的“系统调用”，赋予了 Agent 更高的鲁棒性和可控性；而 GLM/豆包等遵循的 OpenAI 兼容 Function Calling 则是一种“函数调用”范式，设计简洁、生态广泛、易于集成，但对工具执行的管控能力较弱，难以优雅地处理权限、并发、流式等复杂场景，且存在额外延迟开销。简言之，前者以复杂度换取可控性与可靠性，适合构建需要执行高风险操作的复杂 Agent；后者以简洁性换取生态兼容性与低接入成本，适合快速原型开发和对控制力要求不高的场景。

2.Openhands的状态机设计有什么优缺？更优雅的实现方式是怎么样的？
    关于 OpenHands 状态机：OpenHands 通过 AgentController 状态机管理 Agent 生命周期，其优点在于状态可持久化从而支持断点恢复、流程控制清晰避免了标志位混乱、生成与执行解耦增强了安全性，但缺点是状态机相对死板可能限制 LLM 的探索灵活性，且层级抽象带来一定性能开销。更优雅的实现方式不是抛弃状态机，而是对其进行演进：采用事件溯源存储不可变事件以提供完整审计轨迹和时间旅行能力；引入分层状态机将全局状态与局部子任务状态分离以精细化控制；结合基于意图的规划器让状态机负责执行、规划器负责宏观决策以解决死板问题；并在关键状态转换节点引入 LLM 弹性路由，由模型自主判断重试、回滚或上报，从而构建一个既健壮可控又灵活智能的 Agent 系统。

